#find min in list using rec
def find_min(index,nums):
    if index == len(nums)-1:
        return nums[index]
    min = find_min(index + 1,nums)
    if nums[index] < min:
        return nums[index]
    else:
        return min
nums  = [9,9,9,4]
res = find_min(0,nums)
print(res)
