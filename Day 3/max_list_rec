#find max in list using rec
def find_max(index,nums):
    if index == len(nums)-1:
        return nums[index]
    max = find_max(index + 1,nums)
    if nums[index] >  max:
        return nums[index]
    else:
        return max
nums  = [1,2,3,4]
res = find_max(0,nums)
print(res)
