# Counting Sort
"""it works for integers and also
sorts the list if it contains n no of duplicates"""
def counting_sort(nums):
    max_val = max(nums)
    count = [0]*(max_val+1)#creating index 0 to 3
    #[0,0,0,0]
    # 0 1 2 3
    for i in nums:
        count[i] += 1 #we will get count array filled with count values[2,4,3,2]
    #prefix sum
    for i in range(1,len(count)):#update prefix sum in count list again
        count[i] += count[i-1] #[2,6,9,11] in count we have this at this step count is changed to this at this step
    sorted_list = [0] * len(nums)
    for val in reversed(nums):
        sorted_list[count[val]-1] = val # one val stored
        count[val] -= 1 #so reduce prefix count of that element
    return sorted_list
nums=[2,3,1,2,2,3,1,0,1,1,0]
res = counting_sort(nums)
print(res)
