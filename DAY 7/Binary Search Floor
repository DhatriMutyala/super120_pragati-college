#binary search floor
def floor(nums,k):
    l,r = 0,len(nums) - 1
    res = -1
    while l <= r:
        mid = (l + r) // 2

        if nums[mid] < k:
            res = mid
            l = mid + 1
        else:
            r = mid - 1
    return nums[res]

nums = [3,4,5,6,12]
k = 5
print(floor(nums,k))
