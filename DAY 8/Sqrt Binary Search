# sqrt using binary search
n = 48
def square_root(n):
    l = 1
    r = n
    ans = 0
    while l <= r:
        mid = (l + r) // 2
        if mid * mid >= n:
            ans = mid
            r = mid - 1
        else:
            l = mid + 1
    return ans
print(square_root(n))
