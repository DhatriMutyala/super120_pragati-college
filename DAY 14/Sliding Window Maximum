# Sliding Window Maximum (239 LC)
"""Deque (Double-Ended Queue): Used to store indices of elements in a way that the front always holds the index of the maximum element in the current window.

Monotonic Queue: Maintains elements in decreasing order so the max is always at the front."""
import collections
def maxSlidingWindow(nums,k):
     res = []
     queue = collections.deque()
     l = 0
     for r in range(len(nums)):
         while queue and nums[queue[-1]] < nums[r]: #pop elements from queue that are less than cur r num
             queue.pop()
         queue.append(r)
         if l > queue[0]: # if l increses then del prev l frm queue
             queue.popleft()
         if (r + 1) >= k:
             res.append(nums[queue[0]])
             l += 1
     return res
nums = [1,3,-1,-3,5,3,6,7]
k = 3
print(maxSlidingWindow(nums,k))

