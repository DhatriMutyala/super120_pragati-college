# 2Sum using Hash map
def twosum(nums,target):
    hashmap = {}
    for i in range(len(nums)):
        rem = (target - nums[i])
        if rem in hashmap:
            return [hashmap[rem] , i]
        hashmap[nums[i]] = i

nums = [2,7,11,15]
target = 9
print(twosum(nums,target))
